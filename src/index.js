import React from "react";
import ReactDOM from "react-dom";

import BookingAppContainer from "./components/BookingAppContainer";
import "./App.css";

// Render the container which contains the entire app
ReactDOM.render(<BookingAppContainer/>, document.getElementById("root"));